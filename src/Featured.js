import React, { useState } from 'react';


function Featured({ title, imgURL, count, checkFeatured}) {
    return(
        <div>
            <h2>{title}</h2>
        </div>
    )
}

export default Featured;